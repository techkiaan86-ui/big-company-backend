const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function check() { const user = await prisma.user.findFirst({ where: { email: 'gisozi.retailer@big.co.rw' } }); if (!user) return console.log('not found'); const consumer = await prisma.consumerProfile.findFirst({ where: { userId: user.id } }); const retailer = await prisma.retailerProfile.findFirst({ where: { userId: user.id } }); console.log({ userRole: user.role, hasConsumer: !!consumer, hasRetailer: !!retailer }); } check().finally(() => prisma.$disconnect());
